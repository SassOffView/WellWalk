# ─── MindStep Backend — Variabili d'ambiente ─────────────────────────────────
# Copia in .env e inserisci i valori reali. Non committare mai .env!

# ── Server ────────────────────────────────────────────────────────────────────
PORT=3000
NODE_ENV=production

# ── Sicurezza: segreto condiviso con l'app Flutter ───────────────────────────
# L'app invia questo header: X-App-Secret: <valore>
# Deve corrispondere esattamente. Usa una stringa lunga e casuale.
APP_SECRET=cambia-questo-con-una-stringa-segreta-lunga

# ── Rate limiting ─────────────────────────────────────────────────────────────
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=30

# ── Google Gemini ─────────────────────────────────────────────────────────────
GEMINI_API_KEY=AIza...
GEMINI_MODEL=gemini-1.5-flash

# ── OpenAI ChatGPT ────────────────────────────────────────────────────────────
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini

# ── Anthropic Claude ──────────────────────────────────────────────────────────
CLAUDE_API_KEY=sk-ant-...
CLAUDE_MODEL=claude-haiku-4-5-20251001

# ── Deployment (esempi) ───────────────────────────────────────────────────────
# Railway:  https://railway.app → aggiungi queste variabili nell'env del progetto
# Render:   https://render.com  → Environment Variables nel dashboard
# Fly.io:   fly secrets set GEMINI_API_KEY=... etc.
