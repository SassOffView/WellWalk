# ─── Backend AI Proxy — Configurazione ──────────────────────────────────────
#
# Questo file .env.example documenta le variabili da impostare nel
# backend di MindStep (server Node.js / Firebase Functions / altro).
#
# L'app NON contiene API key. Tutte le chiamate AI passano
# attraverso il backend proxy che gestisce le chiavi in modo sicuro.
#
# WORKFLOW:
#   1. Copia questo file in `.env` (non committare mai .env)
#   2. Inserisci le tue API key reali
#   3. Deploy del backend
#   4. Aggiorna BACKEND_BASE_URL nell'app (lib/core/config/app_config.dart)
# ─────────────────────────────────────────────────────────────────────────────

# ── URL del backend (da configurare nell'app) ─────────────────────────────────
BACKEND_BASE_URL=https://api.tuodominio.com/v1

# ── Google Gemini ─────────────────────────────────────────────────────────────
GEMINI_API_KEY=AIza...
GEMINI_MODEL=gemini-1.5-flash

# ── OpenAI ChatGPT ────────────────────────────────────────────────────────────
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini

# ── Anthropic Claude ──────────────────────────────────────────────────────────
CLAUDE_API_KEY=sk-ant-...
CLAUDE_MODEL=claude-haiku-4-5-20251001

# ── Weather (wttr.in — nessuna API key necessaria) ────────────────────────────
# wttr.in è gratuito e non richiede API key per uso moderato.
# Per produzione con alto volume, considerare OpenWeatherMap:
# OPENWEATHER_API_KEY=...

# ── Endpoints del backend proxy attesi dall'app ───────────────────────────────
#
#   POST /ai/insight
#     Body: { provider: "gemini"|"openai"|"claude", prompt: string, userId: string }
#     Response: { insight: string, brainstormPrompt: string, walkTip: string }
#
#   POST /ai/coach
#     Body: { provider: string, messages: [{role, content}], userId: string }
#     Response: { reply: string }
#
#   POST /ai/export
#     Body: { text: string, provider: string, userId: string }
#     Response: { analysis: string }
#
# ── Sicurezza ─────────────────────────────────────────────────────────────────
# Proteggere gli endpoint con autenticazione (es. Firebase Auth JWT).
# L'app invia il token Firebase Auth nell'header Authorization.
# Il backend verifica il token prima di processare la richiesta.
